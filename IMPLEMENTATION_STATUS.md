# 实现状态（V0.6）

更新时间：2026-07-29

## 已形成可运行闭环

- Chrome 扩展识别视频/图文笔记，使用已有登录态创建专用可见标签页。
- 只读、低频、单任务采集；50/100/500/1000/2000/3000 条模式。
- DOM 优先与页面自身响应的被动观察；不主动构造平台请求。
- 一级评论与有限的重要楼中楼、父子关系、点赞、回复、时间和公开 IP 属地。
- 标签页关闭、用户停止、登录/验证码/访问异常暂停；暂停任务由用户主动恢复。
- 批次校验、幂等接收、稳定本地 ID、去重、时间精度、字段缺失诚实标记。
- 任务目录、断点、审计、诊断、SQLite 元数据和任务历史。
- 单文件 GPT 流程、分批 GPT 分类流程、局部修复和总体语义分析流程。
- 固定分类协议、证据 ID 校验、本地统计、同义次要标签合并和一致性复检。
- 人工复核、楼中楼上下文、原始 AI 结果与修改历史、重新计算。
- 私人 HTML 报告、脱敏分享版、隐私扫描、证据跳转、搜索和筛选。
- 采集/分析/报告版本、时间/IP 聚合、观点簇本地计数和争议关联。
- 6GB 阈值检测；5.8GB 暂停新任务；缓存/过期日志安全清理预览与确认。
- 当前用户级 Native Host 注册、诊断、注销、开发版更新入口和 NSIS Windows 安装包。
- 每任务原始评论永久保留开关；永久任务不进入过期原文候选。
- 安装版/开发版连接诊断，可识别注册丢失、Host 缺失、扩展 ID 错误和其他版本覆盖。

## 仍需真实环境验证

- 1000—3000 条深度采集在不同笔记、不同页面版本下的长时间稳定性。
- 小红书页面结构、评论 ID、时间和 IP 字段变化后的适配器回归。
- 登录失效、验证码、限流和网络断开在真实环境中的全部分支。
- Chrome 多配置、多扩展版本同时存在时的诊断提示。

## 尚未作为正式 V1.0 完成

- Windows 安装包尚未配置商业代码签名证书；当前为未签名的个人使用版本。
- 对超过 7 天评论原文的“保留关键证据后精简”执行器；当前只预览，不自动删除。
- 外部事实核验（按产品定义，第一版仅识别声明类型）。
- 自动控制 ChatGPT 网页（按安全设计明确不开发）。

## V1.1 或明确不在当前范围

- Codex 专用入口、Android 系统分享、设备配对和离线提交。
- 账号主页/话题批量、多笔记对比、长期趋势、云同步和多人协作。
- OpenAI/DeepSeek API、PDF/Excel 导出、移动应用和自动更新。
